node {
   stage 'Test Packer version'
         sh 'packer version'
   stage 'Test Teraform version'
         sh 'terraform version'
   stage 'Test Terraform init'
         sh 'terraform init'
   stage 'Test Terraform plan'
         sh 'terraform plan'      
}
